"""
Phase 3: Normalization (skeleton)
현재는 스텁만 유지합니다. 실제 구현은 추후 통합 예정.
"""


def normalize_folder(input_root: str, output_root: str) -> int:
    raise NotImplementedError("normalize_folder is a skeleton; pipeline integration pending.")


if __name__ == "__main__":
    raise SystemExit("normalize_folder is a skeleton; no standalone execution.")
